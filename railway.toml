[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python scheduler.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[nixpacks]
aptPackages = ["ffmpeg", "fonts-dejavu-core", "python3-pip"]
